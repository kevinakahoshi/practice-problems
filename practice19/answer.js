function sort_object_list_by_name(input_array) {
  const output_array = [];
  let temporary_storage = null;

  for (let index = 0; index < input_array.length; index++) {
    if (input_array[index].name < input_array[index + 1].name) {

    }
  }

  return output_array;
}

sort_object_list_by_name();
